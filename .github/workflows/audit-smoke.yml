name: Deployment release smoke

on:
  workflow_dispatch:
    inputs:
      origin:
        description: Exact HTTPS deployment origin to inspect
        required: true
        default: https://fewgoodcoins.xyz
        type: string

permissions:
  contents: read

jobs:
  smoke:
    name: GET-only deployment evidence
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out reviewed source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Use Node 24
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 24
          package-manager-cache: false

      - name: Capture GET-only deployment evidence
        env:
          AUDIT_ORIGIN: ${{ inputs.origin }}
        run: node scripts/audit-smoke.mjs --origin "$AUDIT_ORIGIN" --output audit-smoke.json

      - name: Retain deployment evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: trivium-deployment-smoke-${{ github.sha }}
          path: audit-smoke.json
          if-no-files-found: error
          retention-days: 90
